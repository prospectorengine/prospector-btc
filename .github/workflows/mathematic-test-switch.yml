# =================================================================
# APARATO: MATHEMATIC TEST SWITCH (V1.5 - SSoT ALIGNED)
# CLASIFICACIÓN: OPS INFRASTRUCTURE (ESTRATO L6)
# RESPONSABILIDAD: CERTIFICACIÓN MANUAL DEL NÚCLEO MATEMÁTICO L1
#
# VISION HIPER-HOLÍSTICA 2026:
# 1. VERSION HARMONIZATION: Elimina el error de colisión de pnpm
#    delegando la autoridad total al package.json.
# 2. RUNTIME PRECISION: Sincroniza Node.js 20.x con pnpm 10.26.1.
# 3. ATOMIC INSTALL: Mantiene el blindaje de frozen-lockfile.
# =================================================================

name: "L1: Mathematic Test Switch"

on:
  workflow_dispatch:
    inputs:
      compilation_profile:
        description: 'Perfil de Auditoría (Release recomendado para Criptografía)'
        required: true
        default: 'release'
        type: choice
        options:
          - debug
          - release
      verbose_output:
        description: 'Activar verbosidad forense (--nocapture)'
        required: true
        default: true
        type: boolean

jobs:
  certification:
    name: "L1_Math_Audit"
    runs-on: ubuntu-latest

    timeout-minutes: 30

    steps:
      - name: "🛰️ DNA_ACQUISITION: Checkout Strata"
        uses: actions/checkout@v4

      - name: "⚙️  ENV_STRATIFICATION: Rust Nightly Engine"
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src

      - name: "📦 NODE_SINCRO: Install pnpm"
        uses: pnpm/action-setup@v4
        # ✅ REPARACIÓN: Se remueve el bloque 'with: version' para usar la del package.json

      - name: "🌐 RUNTIME_SETUP: Node.js 20.x"
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: "🧪 I18N_HYDRATION: Generating Dictionaries"
        run: |
          pnpm install --frozen-lockfile
          pnpm i18n:generate

      - name: "💎 CACHE_HANDSHAKE: Restoring Cargo & Nx Strata"
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
            dist
          key: ${{ runner.os }}-l1-math-v1.5-${{ hashFiles('**/Cargo.toml', 'pnpm-lock.yaml') }}

      - name: "💥 EXECUTION: Firing 20 Proving Grounds Tests"
        env:
          ORCHESTRATOR_URL: ${{ secrets.ORCHESTRATOR_URL }}
          WORKER_AUTH_TOKEN: ${{ secrets.WORKER_AUTH_TOKEN }}
          RUST_BACKTRACE: 1
        run: |
          PROFILE_FLAG=""
          if [ "${{ github.event.inputs.compilation_profile }}" == "release" ]; then
            PROFILE_FLAG="--release"
          fi

          VERBOSE_FLAG=""
          if [ "${{ github.event.inputs.verbose_output }}" == "true" ]; then
            VERBOSE_FLAG="-- --nocapture"
          fi

          echo "🚀 Starting L1 Core Math Certification..."
          cargo test --package prospector-core-math $PROFILE_FLAG $VERBOSE_FLAG

      - name: "🏁 VERDICT: Final Strata Report"
        if: always()
        run: |
          echo "Mathematical Integrity sequence finalized."
