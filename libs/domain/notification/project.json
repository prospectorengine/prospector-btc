{
  "name": "domain-notification",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/domain/notification/src",
  "targets": {
    "build": {
      "executor": "@monodon/rust:build",
      "outputs": ["{options.target-dir}"],
      "options": { "target-dir": "dist/target/libs/domain/notification" }
    },
    "lint": { "executor": "@monodon/rust:lint" },
    "test": { "executor": "@monodon/rust:test" }
  },
  "tags": ["layer:domain", "type:logic"]
}
