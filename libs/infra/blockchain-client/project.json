{
  "name": "prospector-blockchain-client",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/infra/blockchain-client/src",
  "targets": {
    "build": {
      "executor": "@monodon/rust:build",
      "outputs": ["{options.target-dir}"],
      "options": { "target-dir": "dist/target/libs/infra/blockchain-client" }
    },
    "test": {
      "executor": "@monodon/rust:test",
      "options": { "target-dir": "dist/target/libs/infra/blockchain-client" }
    },
    "lint": { "executor": "@monodon/rust:lint" }
  },
  "tags": ["layer:infra", "type:client"]
}
