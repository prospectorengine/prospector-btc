Física del Aparato: El FieldElement gestiona la aritmética en un espacio circular definido por el primo
p
p
. La optimización de Solinas permite que, tras una multiplicación que genera 512 bits, "pleguemos" los 256 bits superiores multiplicándolos por
K y sumándolos a los inferiores. Esto evita la división entera, que es la operación más costosa de una CPU. Esta versión V150 incluye trazas para que el Neural Socket pueda informar si las ráfagas de Montgomery están sufriendo latencia inusual.

 ---

