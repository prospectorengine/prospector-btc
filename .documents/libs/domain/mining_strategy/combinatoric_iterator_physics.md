# 📘 CONCEPTO: ITERADOR COMBINATORIO DE ENTROPÍA

**Módulo Objetivo:** `CombinatoricIterator` (L2)

## 1. Física de la Búsqueda
A diferencia del barrido puro en la curva, el iterador combinatorio permite auditar "Patrones de Semilla". Si un operador sospecha que un conjunto de claves fue generado inyectando un ID de hardware en una frase, este aparato permite simular esa ráfaga con precisión de 256 bits.

## 2. Sincronía Nominal (Resolución E0432)
El aparato ha sido nivelado para consumir `add_u64_to_u256_big_endian` y `compare_u256_big_endian`. Esto no es solo un cambio estético; asegura que la lógica de comparación de rangos use el motor ASM optimizado para la CPU, garantizando que el "salto" entre candidatos ocurra en nanosegundos.

## 3. Eficiencia de Memoria
El uso de `String::with_capacity` es una directiva de Tesis para evitar la fragmentación de la memoria en el enjambre distribuido. Al pre-alocar el tamaño exacto del candidato, reducimos los ciclos de recolector de basura simulados y maximizamos el tiempo de CPU dedicado a SHA256.
